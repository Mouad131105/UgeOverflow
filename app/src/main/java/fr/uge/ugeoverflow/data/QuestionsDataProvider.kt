package fr.uge.ugeoverflow.data

import fr.uge.ugeoverflow.model.*
import java.util.*

object QuestionsDataProvider {
    /*val questionLists = listOf(
        QuestionBuilder.builder().title("How to make a great R reproducible example")
            .tags(mutableSetOf(Tag(UUID.randomUUID(),TAG_TYPE.css),Tag(UUID.randomUUID(),TAG_TYPE.java))).
            content(ContentBuilder.builder().
            text("our code should exactly reproduce the problem on another machine by simply copying and pasting it.").build()).
            answers(mutableSetOf(Answer(mutableSetOf(Vote(VOTE_TYPE.UPVOTE,User(UUID.randomUUID()
                ,"Issam","GRINI","Grinio","issamgrn1@gmail.com",
                AddressBuilder.builder().street("avenue président wilson").city("Montreuil").country("France").build()))),
                ContentBuilder.builder().
                text(" I would like to add to the current installed image editor or previewer like Luna Paint, by just adding a button to the editor/title.").build()))).build(),

        QuestionBuilder.builder().title("What is a NullPointerException, and how do I fix it?")
            .tags(mutableSetOf(Tag(UUID.randomUUID(),TAG_TYPE.spring),Tag(UUID.randomUUID(),TAG_TYPE.javascript))).
            content(ContentBuilder.builder().
            text("What i am trying to do - is to save post to the model with custom conditions. But each time im trying to save - i receive this error django.db.utils.IntegrityError: null value in column violates not-null constraint  So far, i have tried editing my models, and adding null=True in parameters, but it doesn't seem to help. So what is the best way to solve the problem here? Also have to mention that it doesnt save event_code at all. But even if i will enter that code by hand - it still will give me \"Connection closed by server.\" Also i dont know what this exactly means: Exception Location:  /usr/local/lib/python3.11/site-packages/redis/asyncio/connection.py,  serialier.py").build()).
            answers(mutableSetOf(Answer(mutableSetOf(Vote(VOTE_TYPE.UPVOTE,User(UUID.randomUUID()
                ,"Toto","TATA","toto_dev","toto@gmail.com",
                AddressBuilder.builder().street("avenue président wilson").city("Montreuil").country("France").build()))),
                ContentBuilder.builder().
                text("our code should exactly reproduce the problem on another machine by simply copying and pasting it.").build()))).build(),

        QuestionBuilder.builder().title("removing duplicates of second column of csv via sort")
            .tags(mutableSetOf(Tag(UUID.randomUUID(),TAG_TYPE.dev),Tag(UUID.randomUUID(),TAG_TYPE.linux))).
            content(ContentBuilder.builder().
            text("My program correctly shows the list of meanings in checkbox from all items for choose. And when I choose option in checkbox and press a button, I have the following error: website shows nothing of results, but there are some items with checked option").build()).
            answers(mutableSetOf(Answer(mutableSetOf(Vote(VOTE_TYPE.DOWNVOTE,User(UUID.randomUUID()
                ,"Feri","Gabriel","gab","gab@gmail.com",
                AddressBuilder.builder().street("avenue président wilson").city("Montreuil").country("France").build()))),
                ContentBuilder.builder().
                text("Combined with the minimal data (see above)").build()))).build(),

        QuestionBuilder.builder().title("removing duplicates of second column of csv via sort")
            .tags(mutableSetOf(Tag(UUID.randomUUID(),TAG_TYPE.dev),Tag(UUID.randomUUID(),TAG_TYPE.linux))).
            content(ContentBuilder.builder().
            text("My program correctly shows the list of meanings in checkbox from all items for choose. And when I choose option in checkbox and press a button, I have the following error: website shows nothing of results, but there are some items with checked option").build()).
            answers(mutableSetOf(Answer(mutableSetOf(Vote(VOTE_TYPE.DOWNVOTE,User(UUID.randomUUID()
                ,"Feri","Gabriel","gab","gab@gmail.com",
                AddressBuilder.builder().street("avenue président wilson").city("Montreuil").country("France").build()))),
                ContentBuilder.builder().
                text("Combined with the minimal data (see above)").build()))).build(),

        QuestionBuilder.builder().title("removing duplicates of second column of csv via sort")
            .tags(mutableSetOf(Tag(UUID.randomUUID(),TAG_TYPE.dev),Tag(UUID.randomUUID(),TAG_TYPE.linux))).
            content(ContentBuilder.builder().
            text("My program correctly shows the list of meanings in checkbox from all items for choose. And when I choose option in checkbox and press a button, I have the following error: website shows nothing of results, but there are some items with checked option").build()).
            answers(mutableSetOf(Answer(mutableSetOf(Vote(VOTE_TYPE.DOWNVOTE,User(UUID.randomUUID()
                ,"Feri","Gabriel","gab","gab@gmail.com",
                AddressBuilder.builder().street("avenue président wilson").city("Montreuil").country("France").build()))),
                ContentBuilder.builder().
                text("Combined with the minimal data (see above)").build()))).build(),

        QuestionBuilder.builder().title("removing duplicates of second column of csv via sort")
            .tags(mutableSetOf(Tag(UUID.randomUUID(),TAG_TYPE.dev),Tag(UUID.randomUUID(),TAG_TYPE.linux))).
            content(ContentBuilder.builder().
            text("My program correctly shows the list of meanings in checkbox from all items for choose. And when I choose option in checkbox and press a button, I have the following error: website shows nothing of results, but there are some items with checked option").build()).
            answers(mutableSetOf(Answer(mutableSetOf(Vote(VOTE_TYPE.DOWNVOTE,User(UUID.randomUUID()
                ,"Feri","Gabriel","gab","gab@gmail.com",
                AddressBuilder.builder().street("avenue président wilson").city("Montreuil").country("France").build()))),
                ContentBuilder.builder().
                text("Combined with the minimal data (see above)").build()))).build(),
*/
        QuestionBuilder.builder().title("removing duplicates of second column of csv via sort")
            .tags(mutableSetOf(Tag(UUID.randomUUID(),TAG_TYPE.dev),Tag(UUID.randomUUID(),TAG_TYPE.linux))).
            content(ContentBuilder.builder().
            text("My program correctly shows the list of meanings in checkbox from all items for choose. And when I choose option in checkbox and press a button, I have the following error: website shows nothing of results, but there are some items with checked option").build()).
            answers(mutableSetOf(Answer(mutableSetOf(Vote(VOTE_TYPE.DOWNVOTE,User(5
                ,"Feri","Gabriel","gab","gab@gmail.com",
                "avenue président wilson Montreuil France"))),
                ContentBuilder.builder().
                text("Combined with the minimal data (see above)").build()))).build()
    )*/
}